# Lefthook configuration
# https://github.com/evilmartians/lefthook
assert_lefthook_installed: true
pre-commit:
  parallel: true
  jobs:
    - name: secrets
      run: bun run env:scan --staged
    - name: lint
      glob: '*.{js,jsx,ts,tsx}'
      run: bun run lint:fix
      stage_fixed: true
    - name: format
      glob: '*.{js,jsx,ts,tsx}'
      run: bun run format
      stage_fixed: true
